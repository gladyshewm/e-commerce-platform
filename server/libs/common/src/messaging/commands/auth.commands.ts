export enum AuthCommands {
  ValidateUser = 'auth.validate-user',
  ValidateUserOAuth = 'auth.validate-user-oauth',
  Register = 'auth.register',
  Login = 'auth.login',
  Refresh = 'auth.refresh',
  Logout = 'auth.logout',
  LogoutAll = 'auth.logout-all',
}
